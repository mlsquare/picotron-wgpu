//! GPU kernels for PicoTron

use anyhow::Result;

/// GPU kernels manager
pub struct GPUKernels;

impl GPUKernels {
    /// Create new GPU kernels manager
    pub fn new() -> Self {
        Self
    }
}
